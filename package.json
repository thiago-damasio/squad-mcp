{
  "name": "squad-mcp",
  "version": "2.0.0",
  "description": "Multi-agent coordination for Claude Code — skill-first architecture with optional MCP server addon",
  "type": "module",
  "bin": {
    "squad": "./bin/squad.js"
  },
  "scripts": {
    "install-global": "npm link",
    "serve": "node src/addon/server.js"
  },
  "keywords": ["claude-code", "multi-agent", "orchestrator", "skill", "mcp", "coordination"],
  "license": "MIT",
  "files": [
    "bin/",
    "skill/",
    "hooks/",
    "schema/",
    "src/addon/"
  ],
  "dependencies": {
    "commander": "^13.0.0"
  },
  "optionalDependencies": {
    "@modelcontextprotocol/sdk": "^1.25.0",
    "express": "^5.1.0",
    "zod": "^3.25.0"
  }
}
